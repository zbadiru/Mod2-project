<%= form_for(@exercise) do |f|%>

    <%= f.label :name %>
    <%= f.text_field :name %><br>

    <%= f.label :routine %>
    <%= f.collection_select :routine_id, Routine.all, :id, :name, prompt: true%> <br>

    <%= f.submit %>
<% end %>